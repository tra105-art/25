# My Web App - Android WebView APK Project

This Android project was generated by **APK Creator Studio**.

## Project Details
- **App Name**: My Web App
- **Package Name**: com.mycompany.webapp
- **Target URL**: https://example.com
- **Version**: 1.0.0 (code: 1)
- **Orientation**: auto_rotate
- **Cache Mode**: default_cache

## How to Build the APK
### Option 1: Automate via GitHub Actions (Recommended)
1. Create a new GitHub repository (e.g. `https://github.com/your-username/my-web-app`).
2. Push this folder to your repository:
   ```bash
   git init
   git add .
   git commit -m "Initial APK Project Setup"
   git branch -M main
   git remote add origin YOUR_GITHUB_REPO_URL
   git push -u origin main
   ```
3. Go to the **Actions** tab in your GitHub repository.
4. The workflow will automatically run and compile the APK!
5. Download the finished **`My_Web_App-v1.0.0.apk`** from Artifacts.

### Option 2: Build locally in Android Studio
1. Open Android Studio.
2. Select **Open** and select this directory.
3. Wait for Gradle sync to finish.
4. Click **Build > Build Bundle(s) / APK(s) > Build APK(s)**.
